---
import { siteCopy } from "../data/translations";

interface Props {
  lang: "en" | "bg";
  successPath: string;
}

const { lang, successPath } = Astro.props;
const copy = siteCopy[lang];
---

<form
  name="contact"
  method="POST"
  data-netlify="true"
  netlify-honeypot="bot-field"
  action={successPath}
  class="space-y-8 border-t border-black/10 pt-8"
>
  <input type="hidden" name="form-name" value="contact" />
  <input type="hidden" name="bot-field" />

  <div class="grid gap-6 sm:grid-cols-2">
    <label class="space-y-2 text-sm text-[#111111]">
      <span>{lang === "en" ? "Name" : "Име"}</span>
      <input class="w-full border-b border-black/15 bg-transparent px-0 py-3 text-base text-[#111111] outline-none transition placeholder:text-[rgba(17,17,17,0.3)] focus:border-[#111111]" type="text" name="name" required />
    </label>
    <label class="space-y-2 text-sm text-[#111111]">
      <span>{lang === "en" ? "Company" : "Компания"}</span>
      <input class="w-full border-b border-black/15 bg-transparent px-0 py-3 text-base text-[#111111] outline-none transition placeholder:text-[rgba(17,17,17,0.3)] focus:border-[#111111]" type="text" name="company" />
    </label>
    <label class="space-y-2 text-sm text-[#111111]">
      <span>Email</span>
      <input class="w-full border-b border-black/15 bg-transparent px-0 py-3 text-base text-[#111111] outline-none transition placeholder:text-[rgba(17,17,17,0.3)] focus:border-[#111111]" type="email" name="email" required />
    </label>
    <label class="space-y-2 text-sm text-[#111111]">
      <span>{lang === "en" ? "Phone" : "Телефон"}</span>
      <input class="w-full border-b border-black/15 bg-transparent px-0 py-3 text-base text-[#111111] outline-none transition placeholder:text-[rgba(17,17,17,0.3)] focus:border-[#111111]" type="tel" name="phone" />
    </label>
  </div>

  <label class="space-y-2 text-sm text-[#111111]">
    <span>{lang === "en" ? "Inquiry type" : "Тип запитване"}</span>
    <select class="w-full border-b border-black/15 bg-transparent px-0 py-3 text-base text-[#111111] outline-none transition focus:border-[#111111]" name="inquiryType" required>
      <option value="">{lang === "en" ? "Select an option" : "Изберете опция"}</option>
      {copy.forms.inquiryTypes.map((item) => (
        <option value={item}>{item}</option>
      ))}
    </select>
  </label>

  <label class="space-y-2 text-sm text-[#111111]">
    <span>{lang === "en" ? "Message" : "Съобщение"}</span>
    <textarea class="min-h-36 w-full border-b border-black/15 bg-transparent px-0 py-3 text-base text-[#111111] outline-none transition placeholder:text-[rgba(17,17,17,0.3)] focus:border-[#111111]" name="message" required></textarea>
  </label>

  <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-end">
    <button
      type="submit"
      class="text-sm text-[#111111] underline-offset-4 hover:underline focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand"
    >
      {copy.actions.sendInquiry}
    </button>
  </div>
</form>
