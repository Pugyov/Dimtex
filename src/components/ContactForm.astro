---
import { siteCopy } from "../data/translations";

interface Props {
  lang: "en" | "bg";
  successPath: string;
}

const { lang, successPath } = Astro.props;
const copy = siteCopy[lang];
---

<form
  name="contact"
  method="POST"
  data-netlify="true"
  netlify-honeypot="bot-field"
  action={successPath}
  class="contact-form space-y-8"
  data-page-reveal
  style="--reveal-index: 1"
>
  <input type="hidden" name="form-name" value="contact" />
  <input type="hidden" name="bot-field" />

  <div class="contact-form-group grid gap-6 sm:grid-cols-2">
    <label class="space-y-2 text-sm">
      <span>{lang === "en" ? "Name" : "Име"}</span>
      <input class="px-0 py-3 text-base transition" type="text" name="name" required />
    </label>
    <label class="space-y-2 text-sm">
      <span>{lang === "en" ? "Company" : "Компания"}</span>
      <input class="px-0 py-3 text-base transition" type="text" name="company" />
    </label>
    <label class="space-y-2 text-sm">
      <span>Email</span>
      <input class="px-0 py-3 text-base transition" type="email" name="email" required />
    </label>
    <label class="space-y-2 text-sm">
      <span>{lang === "en" ? "Phone" : "Телефон"}</span>
      <input class="px-0 py-3 text-base transition" type="tel" name="phone" />
    </label>
  </div>

  <label class="contact-form-group space-y-2 text-sm">
    <span>{lang === "en" ? "Inquiry type" : "Тип запитване"}</span>
    <select class="px-0 py-3 text-base transition" name="inquiryType" required>
      <option value="">{lang === "en" ? "Select an option" : "Изберете опция"}</option>
      {copy.forms.inquiryTypes.map((item) => (
        <option value={item}>{item}</option>
      ))}
    </select>
  </label>

  <label class="contact-form-group space-y-2 text-sm">
    <span>{lang === "en" ? "Message" : "Съобщение"}</span>
    <textarea class="min-h-36 px-0 py-3 text-base transition" name="message" required></textarea>
  </label>

  <div class="contact-form-group flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-end">
    <button
      type="submit"
      class="button-primary"
    >
      {copy.actions.sendInquiry}
    </button>
  </div>
</form>
